import styled from 'styled-components';
import { DarkTheme } from '../../Styles/colors';

export const HeaderAndSearch = styled.div`
    grid-area:header;
    background:${DarkTheme};
    height:70vh;
`

export const HeaderAndSearchInnerContent = styled.div`
    padding:2rem;

`